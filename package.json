{
  "name": "marketingthatworks-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build-all": "npm run build --workspaces",
    "build:main-service": "npm run build --workspace=main-service",
    "build:embedding-service": "npm run build --workspace=embedding-service",
    "build:shared-lib": "npm run build --workspace=@marketingthatworks/shared-lib"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "lit": "^3.2.1",
    "lit-element": "^4.1.1",
    "marketingthatworks-monorepo": "file:",
    "mongodb": "^6.12.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17"
  }
}
